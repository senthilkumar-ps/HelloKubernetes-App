apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-alb-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "helm-alb-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-alb-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
