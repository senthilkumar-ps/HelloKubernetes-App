apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spingprofile-app.fullname" . }}-test-connection"
  labels:
    {{- include "spingprofile-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spingprofile-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
